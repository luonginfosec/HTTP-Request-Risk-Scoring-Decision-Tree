# Notebooks

```text
train_decision_tree.ipynb
```

Main Colab notebook for:

```text
1. loading data/processed/features.csv
2. training Decision Tree and Random Forest models
3. comparing held-out evaluation metrics
4. running manual request tests
5. testing external QLDT and arbitrary Burp XML logs
```

The notebook expects files in Google Drive:

```text
/content/drive/MyDrive/processed/
```

Required:

```text
features.csv
src/
```

Optional external tests:

```text
qldt_ptit_normal_requests.csv
*.xml
```
